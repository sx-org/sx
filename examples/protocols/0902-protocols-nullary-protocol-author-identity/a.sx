// One author of the nullary `Q` — a single-slot vtable.
Q :: protocol vtable { tag :: (self: *Self) -> i64; }
Holder :: struct { v: i64; }
impl Q for Holder { tag :: (self: *Holder) -> i64 => 100 + self.v; }
own_tag :: () -> i64 { x := Holder{ v = 1 }.(Q, context.allocator); return x.tag(); }
