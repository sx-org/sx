// One of two conflicting impls for the same (i64, Wrap) pair.
@import "modules/std.sx";
@import "./0411-protocols-impl-duplicate-types.sx";

impl Into(Wrap) for i64 {
  convert :: (self: i64, alloc: Allocator = context.allocator) -> Wrap {
    _ = alloc;
    .{ v = self * 10 }
  }
}
