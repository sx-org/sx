// A protocol method declared with a real pointer return (`-> *u8`,
// NOT `-> Self`) returns the raw pointer to the caller without the
// dispatch path auto-dereferencing it.

#import "modules/std.sx";

Proto :: protocol vtable {
  get :: (self: *Self) -> *u8;
}

Impl :: struct {
  val: u8 = 42;
}

impl Proto for Impl {
  get :: (self: *Impl) -> *u8 {
    *self.val
  }
}

main :: () -> i32 {
  imp : Impl = .{};
  p := imp.(Proto, context.allocator);   // owning snapshot of the lvalue
  raw : *u8 = p.get();
  addr_word : u64 = xx raw;
  print("got pointer: {}\n", addr_word != 0);
  0
}
