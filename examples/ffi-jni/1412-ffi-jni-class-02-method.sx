// An instance-method body item inside a `#jni_class` declaration:
// `name :: (self: *Self, args...) -> Ret;` — semicolon terminated, no body.
// Parse-only.

#import "modules/std.sx";

// `Self` here refers to the enclosing `View` type — resolved at
// 2.x sema, not at parse time.
View :: #jni_class("android/view/View") extern {
    getId :: (self: *Self) -> i32;
}

main :: () -> i32 {
    print("parse-only ok\n");
    0
}
