// A JNI method whose return type is not a JNI scalar — unsigned 32/64
// or an aggregate — is a compile error at the call site.

#import "modules/std.sx";

Buf :: #jni_class("java/nio/ByteBuffer") extern {
    get :: (self: *Self) -> u32;
}

g_should_call : bool = false;

unused :: (env: *void, b: *Buf) {
    #jni_env(env) {
        _ := b.get();
    }
}

main :: () -> i32 {
    if g_should_call {
        unused(null, null);
    }
    0
}
