// Value-carrying failable functions (the producer side of the
// error-channel tuple ABI). A `-> (T, !E)` function returns EITHER a value OR
// an error: `return v;` yields the success tuple `{v, 0}` (the compiler appends
// the no-error slot), and `raise .X` yields `{undef, tag}` (value slot
// undefined, error slot = the tag). The result is consumed by
// destructuring `v, err := f()` (which extracts both slots); the value-carrying
// `try` / `catch` consumers are exercised in 1012.

@import "modules/std.sx";

E :: error { Bad; Empty }

parse :: (n: i32) -> (i32, !E) {
    if n < 0  { raise .Bad; }
    if n == 0 { raise .Empty; }
    return n * 10;                       // success → {n*10, 0}
}

main :: () -> i32 {
    r : i32 = 0;

    // The value slot is live only where the error is proven absent:
    // read `v1` under an `if !e1` guard, not after a bare tag-compare.
    v1, e1 := parse(5);                  // success → v1 = 50, e1 = no error
    if !e1 { r = r + v1; }                // success → +50

    v2, e2 := parse(-1);                 // Bad
    if e2 == .Bad   { r = r + 7; }   // true  → +7
    if e2 == .Empty { r = r + 200; }  // false

    v3, e3 := parse(0);                  // Empty
    if e3 == .Empty { r = r + 3; }   // true  → +3

    print("value-failable result: {}\n", r);   // 50 + 7 + 3 = 60
    return r;
}
