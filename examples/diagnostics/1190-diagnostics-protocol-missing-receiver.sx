// A method that omits the explicit receiver is a parse error: the
// receiver `self: *Self`/`self: Self` is the required first parameter.
@import "modules/std.sx";

Show :: constraint {
    show :: () -> string;        // ERROR: missing `self` receiver
}

main :: () -> i32 {
    return 0;
}
