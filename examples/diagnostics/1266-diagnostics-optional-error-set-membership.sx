// Optional wrapping must not bypass membership checking for the payload's
// named error set.

Failure :: error { Broken };

main :: () -> i32 {
    failure : ?Failure = null;
    failure = error.NotFailure;
    0
}
