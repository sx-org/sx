// Signed vs unsigned integers of the same width do not meet at a comparison
// type. Write an explicit cast on one side (`a.(u32) < b`).
#import "modules/std.sx";

main :: () {
    a : i32 = -1;
    b : u32 = 1;
    if a < b { print("lt\n"); }
    if a == b { print("eq\n"); }
}
