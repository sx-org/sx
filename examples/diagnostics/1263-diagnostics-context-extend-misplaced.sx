// `@contextExtend` is a top-level-only directive — the Context is assembled once
// per program, so a function-local declaration is rejected at parse time.
@import "modules/std.sx";

main :: () {
    @contextExtend x: i64 = 0;
}
