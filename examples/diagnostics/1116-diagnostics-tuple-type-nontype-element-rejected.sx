// `Tuple` is an ordinary name. In type position `Tuple(i32, i32)` is not a
// type constructor — it fails as an unknown type.

@import "modules/std.sx";

main :: () -> i32 {
    bad : Tuple(i32, i32) = ---;
    0
}
