// A tuple type at a type-demanding site (`size_of(Tuple(i32, 1))`) lists only
// types; the non-type element `1` is rejected where it stands.

#import "modules/std.sx";

main :: () -> i32 {
    print("bad tuple type size = {}\n", size_of(Tuple(i32, 1)));
    0
}
