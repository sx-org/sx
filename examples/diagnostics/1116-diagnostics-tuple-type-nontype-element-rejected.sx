// A tuple type lists only types; the non-type element `1` is rejected where it
// stands, in the annotation that demands the type.

@import "modules/std.sx";

main :: () -> i32 {
    bad : Tuple(i32, 1) = ---;
    0
}
