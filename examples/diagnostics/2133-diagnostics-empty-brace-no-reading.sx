// An empty `{}` after a call names no type to construct (specs: Trailing
// Blocks), so it is the trailing block — and `tally`'s last parameter takes no
// block, which is what the report names.

tally :: (n: i32) -> i32 {
    return n;
}

main :: () -> i32 {
    return tally(2) {};
}
