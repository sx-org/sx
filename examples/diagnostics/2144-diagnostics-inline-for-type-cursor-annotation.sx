// A type-list cursor binds a type, not a value of one, so it takes no type
// annotation.

#import "modules/std.sx";

TYPES :: .[i32, i64];

main :: () {
    inline for T: i64 in TYPES {
        v : T = ---;
        _ = v;
    }
}
