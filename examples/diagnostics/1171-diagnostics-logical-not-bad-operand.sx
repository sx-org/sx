// `!` on an operand with no truthiness — neither bool, integer, nor an error
// binding — is diagnosed.
#import "modules/std.sx";

main :: () -> i32 {
    s := "text";
    if !s { print("unreachable\n"); }
    return 0;
}
