// `!` on an operand that has no truthiness (neither bool, integer, nor
// an error binding) is diagnosed instead of being silently
// bit-flipped.
#import "modules/std.sx";

main :: () -> i32 {
    s := "text";
    if !s { print("unreachable\n"); }
    return 0;
}
