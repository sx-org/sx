// `@identity` names an ownership class for runtime values. A constraint
// protocol has none, so the attribute is refused at the declaration.
@import "modules/std.sx";

Ord :: constraint {
    less :: (self: *Self, other: Self) -> bool;
}

main :: () { print("unreachable\n"); }
