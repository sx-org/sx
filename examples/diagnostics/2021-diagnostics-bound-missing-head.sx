// Every `/` in a bound list introduces one bound, so a trailing slash has no
// protocol head to name.
#import "modules/std.sx";

first :: (a: $T/) -> i32 { 1 }

main :: () {
    print("{}\n", first(1));
}
