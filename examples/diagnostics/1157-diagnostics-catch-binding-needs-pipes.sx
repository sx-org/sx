// The catch error binding sits in pipes — `catch |e| {}`; a bare binding
// is rejected with a fixit.

@import "modules/std.sx";

E :: error { Bad };

f :: () -> (i64, !E) { raise .Bad; }

main :: () {
    v := f() catch e { 0 };
    print("{}\n", v);
}
