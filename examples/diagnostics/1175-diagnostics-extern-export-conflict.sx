// `extern` and `export` are the two values of one linkage axis — a declaration is
// either an import or a definition, never both. The parser rejects the redundant
// second keyword with the caret on it.

f :: (a: i32) -> i32 extern export;

main :: () -> i32 { 0 }
