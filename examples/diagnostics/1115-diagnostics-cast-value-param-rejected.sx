// `x.(T)` where `T` is a value parameter declared `: Type` (without the `$`
// generic sigil) is rejected with the generic-param hint.
conv :: (T: Type, x: i32) -> i32 {
    return x.(T);
}

main :: () -> i32 {
    return conv(i32, 5);
}
