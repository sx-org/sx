// `.*` on a non-pointer value is rejected at the deref site.

Point :: struct { x: i32; y: i32; }

main :: () -> i32 {
    p : Point = .{ x = 3, y = 4 };
    q := p.*;          // ERROR: `p` is a Point value, not a pointer
    return q.x;
}
