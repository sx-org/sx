// `#inline` is not a protocol attribute. It is recognized nowhere; it fails
// through the ordinary parse path, with no hint arm.
#import "modules/std.sx";

Hasher :: protocol #inline {
    sum :: (self: *Self) -> u64;
}

main :: () { print("unreachable\n"); }
