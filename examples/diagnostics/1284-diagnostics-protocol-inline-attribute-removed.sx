// The layout is spelled in the kind slot (`protocol inline`). `#inline` is not
// recognized anywhere — it fails through the ordinary lexer path, with no hint
// arm for it.
#import "modules/std.sx";

Hasher :: protocol #inline {
    sum :: (self: *Self) -> u64;
}

main :: () { print("unreachable\n"); }
