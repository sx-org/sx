// `inline` is not a protocol kind. The kind slot admits `constraint` and
// `vtable`; the keyword standing there is refused by the head's mandatory `{`.
#import "modules/std.sx";

Hasher :: protocol inline {
    sum :: (self: *Self) -> u64;
}

main :: () { print("unreachable\n"); }
