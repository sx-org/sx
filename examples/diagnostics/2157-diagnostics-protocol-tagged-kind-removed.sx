// `tagged` is not a protocol kind. It stands in the kind slot as an ordinary
// unrecognized word, and the parse error names the kinds that exist.
#import "modules/std.sx";

View :: protocol tagged {
    area :: (self: *Self) -> i64;
}

main :: () { print("unreachable\n"); }
