// An unknown type in a NESTED closure body's local annotation is rejected, with
// the enclosing function's generic params still in scope.
main :: () -> i32 {
    f := () -> i32 {
        bad: Coordnate = ---;
        return 0;
    };
    return f();
}
