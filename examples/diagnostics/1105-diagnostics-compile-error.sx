// `compile_error(...)` was consolidated into the `#error("msg");` directive
// (fires when reached in live code, anchored at the outermost instantiation
// site). The removed spelling gets a migration diagnostic, not a silent
// unknown-name fallthrough.

#import "modules/std.sx";

#run compile_error("this spelling is gone");

main :: () { }
