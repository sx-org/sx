// An identifier in a type position that names no declared type, builtin, or
// in-scope generic parameter is rejected — here at a struct field.
Point :: struct {
    x: i32;
    y: Coordnate;   // typo for a non-existent type
}

main :: () -> i32 {
    return 0;
}
