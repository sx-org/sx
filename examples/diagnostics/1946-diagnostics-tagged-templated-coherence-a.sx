// One impl of the `(Series(f32), Sine)` pair.
#import "modules/std.sx";
#import "./1946-diagnostics-tagged-templated-coherence-types.sx";

impl Series(f32) for Sine { at :: (self: *Sine, i: i64) -> f32 { self.freq } }
