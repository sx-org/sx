// A private field cannot be assigned by name from another file.

box :: @import "2200-diagnostics-private-field-write/box.sx";

main :: () -> i32 {
    x := box.make();
    x.secret = 1;
    box.g.secret = 2;
    box.g.secret, n = 3, 4;
    return 0;
}
