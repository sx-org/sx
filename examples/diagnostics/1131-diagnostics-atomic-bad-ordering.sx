// An atomic load with an ordering LLVM forbids (.release / .acqRel) is rejected
// with an sx diagnostic.
@import "modules/std.sx";
@import "modules/std/atomic.sx";

main :: () {
    n : i64 = 0;
    x := atomicLoad(i64, *n, .release);
}
