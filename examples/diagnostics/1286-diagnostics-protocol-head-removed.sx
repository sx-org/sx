// `protocol` is an ordinary identifier: a declaration spelling it in the head
// slot fails through the ordinary parse path, exactly as any other identifier
// standing where a declaration's value belongs.
@import "modules/std.sx";

Ord :: protocol erased {
    less :: (self: *Self, other: Self) -> bool;
}

main :: () { print("unreachable\n"); }
