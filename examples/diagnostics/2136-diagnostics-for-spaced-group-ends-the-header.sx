// A spaced `(` group before the body ends the iterable list — the loop is
// then missing its body. Glue still binds a call (`for x in f(n)`).

@import "modules/std.sx";

main :: () {
    xs : [2]i64 = .[1, 2];
    for xs (x) { }
}
