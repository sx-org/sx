// A spaced `(` after `..` is the range end, same as a glued `(`.

@import "modules/std.sx";
main :: () -> i32 {
    n := 4;
    c := 0;
    for i in 0.. (n) { c += 1; }
    print("{}\n", c);
    0
}
