// The stage probe is `@isComptime()`. The unprefixed spelling names no
// declaration, so it fails as an ordinary unresolved identifier.
@import "modules/std.sx";

main :: () -> i32 {
    if isComptime() { return 1; }
    return 0;
}
