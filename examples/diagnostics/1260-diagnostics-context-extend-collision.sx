// The Context field namespace is program-global and FLAT — two `@contextExtend`
// declarations with the same field name, here across two modules, are a compile
// error naming BOTH declaration sites: the primary at the later site, a note at the
// first. No merging, no own-wins, no per-source resolution.
@import "modules/std.sx";
@import "1260-diagnostics-context-extend-collision/lib.sx";

@contextExtend tracer: i64 = 1;

main :: () {}
