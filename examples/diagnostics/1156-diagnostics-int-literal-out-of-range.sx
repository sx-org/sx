// An integer literal that does not fit its integer target type is a compile error
// — no silent wrap. Both faces are diagnosed in one run.

#import "modules/std.sx";

main :: () {
    x : i8 = 300;
    print("x: {}\n", x);
    y : u8 = 256;
    print("y: {}\n", y);
}
