// The kind slot follows the parameter list, so a parameterized head refuses
// `tagged` at the same place a bare one does.
#import "modules/std.sx";

Series :: protocol(T: Type) tagged {
    at :: (self: *Self, i: i64) -> T;
}

main :: () { print("unreachable\n"); }
