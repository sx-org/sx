// Swizzle errors: mixed sets, a letter past the receiver, a repeated
// store letter, and address-of a multi-letter shuffle.
#import "modules/std.sx";

main :: () {
    v : @Vector(3, f32) = .[1.0, 2.0, 3.0];
    _ = v.xr;
    _ = v.xyzw;
    v.xx = .[0.0, 0.0];
    _ = *v.xy;
}
