// An inline enum is held to the same rule as a named one: an `else` member
// on a payload enum is diagnosed where the type is spelled, and records no
// member for a construction to reach.
@import "modules/std.sx";

main :: () {
    s : enum { dot; circle: f32; else other; } = .other(1);
    _ = s;
}
