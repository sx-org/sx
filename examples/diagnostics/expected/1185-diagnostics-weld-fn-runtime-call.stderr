error: 'rawIntern' runs only at compile time — it cannot be called from the runtime call graph (use it inside @run or a comptime '::')
  reached from the runtime graph:
    main
    -> rawIntern   (compile-time only)
