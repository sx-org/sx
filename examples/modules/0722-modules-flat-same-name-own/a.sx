// a.sx authors `greet`. Its own `fromA` calls `greet` bare, which binds
// a.sx's OWN author (own-author wins), even though b.sx also
// authors `greet` and the first-wins merge keeps only one in the merged scope.
greet  :: () -> i64 { return 1; }
fromA  :: () -> i64 { return greet(); }
