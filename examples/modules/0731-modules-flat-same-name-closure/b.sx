// b.sx authors its OWN `pick` returning 2. Taking `pick` as a value —
// `f := pick` or `g : () -> i64 = pick` — must capture b.sx's author (2),
// not the first-wins winner from a.sx (1).
pick :: () -> i64 { return 2; }
fromBClosure :: () -> i64 { f := pick; return f(); }
fromBValue   :: () -> i64 { g : () -> i64 = pick; return g(); }
