// No imports: `Into` is not nameable here, so this impl cannot
// register. The importer's build fails loudly at the declaration.
DeadBox :: struct { v: i64; }

gDead : DeadBox = .{ v = 99 };

impl Into(*DeadBox) for string {
    convert :: (self: string) -> *DeadBox { return *gDead; }
}
