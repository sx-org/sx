// A second route to the same package: this module reaches the set's module too,
// so the program holds two paths to one declaration.
@import "modules/std.sx";
v :: @import "view.sx";

sizeViaMid :: () -> i64 => @sizeOf(v.View);
