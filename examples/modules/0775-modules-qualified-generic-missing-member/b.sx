// An unrelated module's generic `Box($T)` (two fields, size 16). The qualified
// head `a.Box(..)` does not reach this template through the bare global
// last-wins map.
Box :: struct($T: Type) { x: T; y: T; }
