// Module A authors `K :: 2` and a struct `Box` whose array field is dimensioned
// by A's `K`. `@sizeOf(Box)` folds the field dimension against A's `K` (= 2).
K :: 2;
Box :: struct { arr: [K]u8; }
aSz :: () -> i64 { return @sizeOf(Box); }
