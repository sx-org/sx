// Companion module for 1620 — a mutable module global reachable through a
// namespace-import alias, plus a const and a fn to prove the store path
// rejects non-lvalue qualified members.
libG : i64 = 0;
LIB_C :: 99;
libFn :: () -> i64 { 7 }
