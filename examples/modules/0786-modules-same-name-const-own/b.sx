// Module B authors a DIFFERENT same-name value const `K` (2) — a shadow of A's
// `K`. Each `K` is selected per declaring source, so B's `bK` reads B's value
// while A's `aK` reads A's, never the global last-wins const.
K :: 2;
bK :: () -> i64 { return K; }
