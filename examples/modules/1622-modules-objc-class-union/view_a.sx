#import "modules/std.sx";
#import "modules/ffi/objc.sx";

// One module's VIEW of NSString — extern @ObjcClass decls are
// C-header-like; same-name views of one runtime class merge into a
// union surface.
Probe :: @ObjcClass("NSString") extern {
    UTF8String :: (self: *Self) -> [*]u8;
}

probe_first_byte :: (p: *void) -> i64 {
    s : *Probe = xx p;
    s.UTF8String()[0].(i64)
}
