// Module B authors a DIFFERENT same-name `K` declared `f64` (a shadow of A's
// `K`). Its inferred-return `bK` reads `K` bare; the inferred return type must
// be B's `f64`, so the value prints as `2.500000`.
K : f64 : 2.5;
bK :: () { return K; }
