// Two namespace targets may author the same inner alias spelling. Nested
// dispatch must retain both outer and inner target identity rather than
// collapsing through a process-global `engineAlias.value` key.

a :: @import "0902-modules-nested-namespace-alias-identity/a.sx";
b :: @import "0902-modules-nested-namespace-alias-identity/b.sx";

main :: () -> i32 {
    if a.engineAlias.value() != 11 { return 1; }
    if b.engineAlias.value() != 22 { return 2; }
    if a.engineAlias.generic(1) != 11 { return 3; }
    if b.engineAlias.generic(1) != 22 { return 4; }
    if a.engineAlias.defaulted() != 31 { return 5; }
    if b.engineAlias.defaulted() != 42 { return 6; }
    0
}
