Record :: struct { aOnly: i64; }

COUNT :: 11;
STATE : i64 = 100;

Kind :: enum { selected; other; }

Box :: struct($T: Type) { aBox: T; }

Make :: ($T: Type) -> Type {
    return struct { aMade: T; };
}
