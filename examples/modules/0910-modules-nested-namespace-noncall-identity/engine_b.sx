Record :: struct { bOnly: i64; }

COUNT :: 22;
STATE : i64 = 200;

Kind :: enum { other; selected; }

Box :: struct($T: Type) { bBox: T; }

Make :: ($T: Type) -> Type {
    return struct { bMade: T; };
}
