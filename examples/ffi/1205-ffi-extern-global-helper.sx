// Companion module for 1205-ffi-extern-global.sx. Declares the same `extern`
// global as the main file; the linker treats both decls as one symbol. The
// trivial fn is what gives this file a call site to participate in the link.

__stdinp : *void extern;

stdinpAddrPresent :: () -> i32 {
    1
}
