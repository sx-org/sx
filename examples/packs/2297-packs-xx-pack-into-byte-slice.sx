// `xx args` with a `[]u8` destination materializes the comptime pack into that
// slice, the type `string` names.
@import "modules/std.sx";

count :: (items: []u8) -> i64 { items.len }

fwd :: (..$args) -> i64 { count(xx args) }

main :: () -> i32 {
    print("{}\n", fwd('a', 'b'));
    0
}
