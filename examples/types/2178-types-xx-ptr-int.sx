// Dest-inferred `xx` reinterprets pointer ↔ integer. Arithmetic that
// produces a number writes the dest on the value: `p.(i64)`.
#import "modules/std.sx";

main :: () {
    n := 1;
    p := *n;
    addr : i64 = xx p;
    back : *i64 = xx addr;
    print("{} {}\n", addr != 0, back.*);
}
