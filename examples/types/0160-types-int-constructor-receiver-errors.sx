// A malformed `@int` as a numeric-limit receiver or an `is` subject reports
// the constructor diagnostic at that receiver, not a generic
// field-not-found.
@import "modules/std.sx";

main :: () -> i32 {
    a := @int(0, .signed).max;
    d := @int(0, .signed) is int;
    return 0;
}
