std :: #import "modules/std.sx";

vec3 :: (x:f32, y:f32, z:f32) -> std.Vector(3, f32) {
    .[x,y,z]
}

main :: () {
    v1 := vec3(1,0,0);
    v2 := vec3(0,0,1);
    s := 0.5;

    sum := (v1 - v2);// math.cross(v1, v2);

    std.print("{}\n", sum);
}
