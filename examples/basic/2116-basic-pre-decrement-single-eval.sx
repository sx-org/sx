#import "modules/std.sx";

calls : i64 = 0;
bump :: () -> i64 {
    calls += 1;
    return 1;
}

main :: () -> i32 {
    xs : [3]i64 = .[10, 20, 30];
    --xs[bump()];
    print("elem={} calls={}\n", xs[1], calls);
    return 0;
}
