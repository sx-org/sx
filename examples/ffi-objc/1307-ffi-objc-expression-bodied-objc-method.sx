// The `=>` expression-body form parses for `@ObjcClass` member methods,
// mirroring `parseFnDecl`'s arrow arm.

#import "modules/std.sx";

SxFoo :: @ObjcClass("SxFoo") {
    greet :: (self: *Self) -> i32 => 42;
}

main :: () -> i32 {
    0
}
