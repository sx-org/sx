// An error tag assigned to an optional of its own error set wraps into that optional.
Failure :: error { Broken };

main :: () -> i32 {
    failure : ?Failure = null;
    failure = error.Broken;
    if failure == null { return 1; }
    0
}
