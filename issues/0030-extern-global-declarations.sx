// The cross-file `extern G : T;` global requested by issue 0030: a reference to
// a global defined in another sx file, resolved at link time. The compiler
// rejects it with "expected '::', ':=', or ':' after identifier" — the form does
// not exist. Distinct from `name : T extern;`, an external C data symbol
// (examples/ffi/1205-ffi-extern-global.sx).

#import "modules/std.sx";

extern g_x : *void;

main :: () -> i32 { 0; }
