BState :: struct { b: bool; }
Thing :: struct {
    state: BState;
    init :: () -> Thing { Thing.{ state = .{ b = true } } }
    reset :: (self: *Thing) { self.state = BState.{ b = false }; }
}
use :: () -> bool {
    value := Thing.init();
    value.reset();
    value.state.b
}
