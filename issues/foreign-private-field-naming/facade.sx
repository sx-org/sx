@import "internal.sx";

Wrapper :: struct {
    private state: Secret;

    init :: (value: i64) -> Wrapper {
        Wrapper{ state = makeSecret(value) }
    }

    get :: (self: *Wrapper) -> i64 { self.state.value }
}
