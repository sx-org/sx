// issue 0211: forward-referencing an error set from a fn signature
// panics the compiler (struct stub adopted by error_set trips the
// updatePreservingKey kind-stability assert). Expect: exit 0.
f :: (e: E) -> i32 { return 0; }
E :: error { Fault }
main :: () -> i32 { return 0; }
